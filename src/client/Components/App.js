import React from 'react';
import { hot } from 'react-hot-loader';
import styles from '../../stylesheets/main.scss';

const App = () => {
  return <div className="start">{'new site'}</div>;
};
export default hot(module)(App);
